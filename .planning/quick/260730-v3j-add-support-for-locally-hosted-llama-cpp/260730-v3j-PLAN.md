---
phase: quick-260730-v3j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - policy.yaml
  - rebuild.sh
  - tests/test-local-model-guard.sh
  - docs/local-models-guidelines.md
  - docs/security-guidelines.md
  - AGENTS.md
  - README.md
autonomous: true
requirements: [NET-06, RUN-07]
user_setup:
  - service: host-side-llama-cpp-proxy
    why: "Anthropic<->OpenAI translation proxy fronting the operator's existing llama.cpp switcher. Explicitly OUT OF SCOPE for this plan (D-03) — delivered as written guidance in docs/local-models-guidelines.md, built by the operator on the host."
    dashboard_config:
      - task: "Confirm the switcher's actual protocol (OpenAI-compatible /v1/chat/completions vs llama.cpp-native /completion)"
        location: "Host-side switcher config — unconfirmed at plan time (D-02)"
      - task: "Determine the host address the sandbox can reach, then replace the placeholder host in policy.yaml and pass it to --base-url"
        location: "Podman/OpenShell host networking — unconfirmed at plan time (D-03); do not guess an IP"

must_haves:
  truths:
    - "Default posture is unchanged: with no operator edits, policy.yaml's active network_policies still contain exactly claude_egress and go_egress (D-01/D-03)."
    - "policy.yaml carries a ready-to-uncomment local_model_egress template that is claude-binary-scoped, has no protocol field, and uses a non-guessed placeholder host (D-01, D-03)."
    - "./rebuild.sh claude-local --base-url <url> launches Claude Code with ANTHROPIC_BASE_URL set at exec time; a missing or malformed URL fails closed with exit 1 before any podman/openshell call (D-01, D-04)."
    - "If the operator uncomments the template, NET-04 fails closed unless local-model-egress is claude-scoped, passthrough, non-empty, and the placeholder host has been replaced (NET-06)."
    - "docs/local-models-guidelines.md tells the operator exactly what to build on the host and which facts are still unverified (D-02, D-03)."
    - "AGENTS.md's docs index and README.md's doc table + verb list reference the new guideline and the claude-local verb."
  artifacts:
    - "policy.yaml — commented local_model_egress template appended inside network_policies"
    - "rebuild.sh — claude-local verb, --base-url flag + validation, assert_local_model_egress_if_present() defined AND called"
    - "tests/test-local-model-guard.sh — negative-path guard test for the --base-url validator"
    - "docs/local-models-guidelines.md — sixth domain guideline"
    - "docs/security-guidelines.md, AGENTS.md, README.md — updated to reference the opt-in path"
  key_links:
    - "Sentinel host token REPLACE-ME-LOCAL-MODEL-HOST is shared between the policy.yaml template (Task 1) and the NET-04 assertion (Task 2) — if they drift, the fail-closed guard silently never fires."
    - "Policy name local-model-egress is the key the NET-04 assertion selects on — must match the template's name: field exactly."
    - "Verb name claude-local must appear in the verb allowlist case, both usage strings, the dispatch arm, and README's verb block — a missing allowlist entry makes the verb unreachable."
---

<objective>
Add sandbox-side support for routing the in-sandbox Claude Code at a host-side, Anthropic-compatible translation proxy that fronts the operator's existing llama.cpp switcher.

**Scope is sandbox-side repo changes ONLY.** Writing, running, or testing host-side proxy code is explicitly out of scope (D-03); the host-side work is delivered as a written setup guide the operator implements outside the sandbox.

Purpose: give the operator an opt-in path to local models without weakening the default two-allowlist Architecture B posture, and without guessing facts (switcher protocol, host-reachable address) that are unconfirmed at plan time.

Output: an opt-in `local_model_egress` policy template, a `claude-local` verb, a fail-closed NET-04 assertion, a negative-path guard test, and `docs/local-models-guidelines.md` plus doc-index updates.

**Decision IDs** — this plan normalizes the unnumbered decision headings in `260730-v3j-CONTEXT.md` as:
- **D-01** = *Proxy location*: proxy runs on the host; sandbox reaches it via `ANTHROPIC_BASE_URL`; the egress addition is security-relevant and must follow `docs/security-guidelines.md` (fail-closed, explicit scoping, no widening of `go_egress`).
- **D-02** = *Switcher protocol*: unconfirmed. Build guidance around the common OpenAI-compatible `/v1/chat/completions` case, flag it as requiring operator confirmation, and note what changes if it is llama.cpp's native `/completion`.
- **D-03** = *Scope*: sandbox-side `policy.yaml` + `rebuild.sh` + docs only; host proxy code out of scope; host reachability is an open question — do not guess an IP.
- **D-04** = *Claude's discretion*: verb/flag naming, new policy block vs `claude_egress` variant, doc location/structure.

**Discretion exercised under D-04:** new independently-scoped policy block `local_model_egress` (follows the `go_egress` precedent rather than widening `claude_egress`); verb `claude-local`; flag `--base-url` with `LOCAL_MODEL_BASE_URL` env fallback; doc at `docs/local-models-guidelines.md` as the sixth guideline in the existing five-guideline pattern.

**Anchor IDs** for code comments and the commit message: `NET-06` (opt-in local-model egress allow + its assertion), `RUN-07` (the `claude-local` launch verb). These are used as in-code anchors per the repo's ID convention; adding them to `.planning/REQUIREMENTS.md` is out of scope for this quick task (the STATE.md Quick Tasks table is the record).

**Executor environment note:** this plan is executed INSIDE the sandbox. `podman`, `openshell`, `shellcheck`, `yq`, and PyYAML are all absent here (verified). Every `<verify>` below is therefore static/structural — `bash -n`, `grep`/`awk`, and exit-code assertions on `rebuild.sh` invocations that fail before reaching any host tool. Do NOT attempt live network calls, `podman`, or `openshell` commands.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/security-guidelines.md
@docs/integration-guidelines.md
@docs/error-handling-guidelines.md
@policy.yaml
@rebuild.sh
@README.md
@.planning/quick/260730-v3j-add-support-for-locally-hosted-llama-cpp/260730-v3j-CONTEXT.md

Read before writing the guard test: `tests/test-pin-held.sh` (house style for negative-path tests — `mktemp -d`, `trap … EXIT`, exact exit-code assertions, `PASS:`/`FAIL:` prefixes).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: policy.yaml — opt-in local_model_egress template (NET-06, D-01/D-03)</name>
  <files>policy.yaml</files>
  <read_first>
    `policy.yaml` lines 97-127 (the `go_egress` block) — copy its shape, indentation, and comment density exactly. `docs/security-guidelines.md` "Editing policy.yaml" rules 1-4.
  </read_first>
  <action>
Append a **fully commented-out** `local_model_egress` template to the end of `policy.yaml`, inside the `network_policies:` mapping (immediately after the `go_egress` block). Commented-out is what makes this opt-in and keeps the default posture byte-identical in the active policy (D-01, D-03).

Formatting contract for the template lines: each line starts with `#` at column 0, followed by exactly one space, followed by the real YAML text **including its own indentation**. Stripping the leading two characters from every line must therefore yield valid YAML at the correct depth. Verify this by eye before finishing — a broken indent here produces a policy file that fails to parse the moment the operator uncomments it.

The commented YAML must mirror `go_egress` structurally: a 2-space-indented `local_model_egress:` key; 4-space `name: local-model-egress`; 4-space `endpoints:` with one 6-space `- host: REPLACE-ME-LOCAL-MODEL-HOST` entry and its `port: 8787`; and 4-space `binaries:` listing `- { path: /usr/bin/claude }` and `- { path: /usr/local/bin/claude }`.

Hard constraints the template must satisfy and state in its own prose comments:
- **No `protocol` key on the endpoint.** Omitting it is what produces opaque TCP/TLS passthrough; adding `protocol: rest` would terminate TLS. Carry the same explanatory comment style used on the `go_egress` endpoints.
- **Claude-binary scope only.** The `binaries:` list contains the two `claude` paths and nothing else. Adding a Go binary here, or adding these paths to `go_egress`, breaks the core isolation invariant.
- **`REPLACE-ME-LOCAL-MODEL-HOST` is a deliberate placeholder, not a hostname.** The host address the sandbox can use to reach the host-side proxy is unconfirmed (D-03) — no IP is guessed. The comment must instruct the operator to determine it, replace the placeholder, replace `8787` with their proxy's port, then uncomment the block and re-run `./rebuild.sh`.
- **A fail-closed guard exists.** State that NET-06 (`assert_local_model_egress_if_present` in `rebuild.sh`) aborts the rebuild if the block is uncommented while the placeholder host is still in place, if any endpoint carries a `protocol` field, if any non-`claude` binary is scoped to it, or if `endpoints:` is empty.
- **Pointer to the guideline.** Reference `docs/local-models-guidelines.md` for the host-side setup this depends on.

Also extend the file's top-of-file header comment (the Architecture B paragraph around lines 15-23) with one short paragraph noting the optional third, claude-scoped allowlist that ships commented-out by default, so a reader of the header is not surprised by the block at the bottom.

Do not touch `filesystem_policy`, `landlock`, `process`, `claude_egress`, or `go_egress`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ACTIVE=$(awk '/^network_policies:/{f=1;next} f' policy.yaml | grep -v '^[[:space:]]*#' | grep -E '^  [A-Za-z_][A-Za-z0-9_]*:' | tr -d ' :' | sort | tr '\n' ' ') && [ "$ACTIVE" = "claude_egress go_egress " ] && echo "PASS: active policies unchanged ($ACTIVE)" && grep -q '^# *local_model_egress:' policy.yaml && grep -q '^# *name: local-model-egress' policy.yaml && grep -q 'REPLACE-ME-LOCAL-MODEL-HOST' policy.yaml && grep -q 'docs/local-models-guidelines.md' policy.yaml && echo "PASS: commented template present"</automated>
  </verify>
  <done>
`policy.yaml`'s active (comment-stripped) `network_policies` region still contains exactly `claude_egress` and `go_egress`. A commented `local_model_egress` template exists with `name: local-model-egress`, the `REPLACE-ME-LOCAL-MODEL-HOST` placeholder, two `claude` binary paths, no `protocol` key, and a pointer to `docs/local-models-guidelines.md`. Stripping `# ` from every template line yields correctly indented YAML.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rebuild.sh — claude-local verb, --base-url validation, NET-06 assertion, guard test (RUN-07/NET-06, D-01/D-04)</name>
  <files>rebuild.sh, tests/test-local-model-guard.sh</files>
  <read_first>
    `rebuild.sh` lines 110-243 (`assert_claude_egress_allowlist` — the fetch-guard + `jq -e` assertion pattern to copy), lines 306-368 (verb allowlist + flag loop), lines 438-468 (the `claude` verb arm to mirror), lines 677-706 (Step 5 call site + closing summary). `docs/error-handling-guidelines.md` §3b (interactive exit-code preservation), §6 (validate-before-assert), §10 (arg parsing). `tests/test-pin-held.sh` for test house style.
  </read_first>
  <behavior>
    - `./rebuild.sh claude-local` with no `--base-url` and no `LOCAL_MODEL_BASE_URL` → exit 1, stderr mentions `--base-url`.
    - `./rebuild.sh claude-local --base-url` (flag present, value missing) → exit 1.
    - `./rebuild.sh claude-local --base-url 'http://x;rm -rf /'` → exit 1 (metacharacter rejected by the allowlist regex).
    - `./rebuild.sh claude-local --base-url 'http://host name:8787'` → exit 1 (whitespace rejected).
    - `./rebuild.sh bogusverb` → still exit 1 with usage (no regression).
    - All of the above must exit 1 **from the URL validator**, before any `podman`/`openshell` call — proven by the fact that they return 1 in this sandbox, where neither tool exists.
  </behavior>
  <action>
Two deliverables: the `rebuild.sh` changes, and a negative-path guard test that locks the validator in place.

**A. `rebuild.sh` — `claude-local` verb (RUN-07).** Touch these locations, all of them:

1. *Header usage block* (lines 4-12): add a `./rebuild.sh claude-local --base-url <url>` line describing it as launching Claude Code against a host-side local-model proxy.
2. *Verb allowlist* (the `case "$1" in` at line 312): add `claude-local` to the alternation. `claude` and `claude-local` are distinct literal patterns, so alternation order is irrelevant — but both must be present or the verb is rejected as unknown.
3. *Both usage strings* (lines 322 and 364): include `claude-local` in the bracketed verb list.
4. *Flag loop* (lines 331-368): add `--base-url` and `--base-url=*` arms following the exact shape of the existing `--since` arms, including the `[[ -z "${2-}" ]]` missing-value guard. Initialize `LOCAL_MODEL_BASE_URL="${LOCAL_MODEL_BASE_URL:-}"` alongside `AUDIT_SINCE=""` so the env-var fallback works and `set -u` cannot abort.
5. *New dispatch arm*, placed immediately after the `claude)` arm. Order of operations inside it is load-bearing:
   - **Validate first, before `ensure_podman_ready`.** If `LOCAL_MODEL_BASE_URL` is empty, `log_error` naming `--base-url` and the `LOCAL_MODEL_BASE_URL` env var, point at `docs/local-models-guidelines.md`, and `exit 1`. Then allowlist-validate the value against `^https?://[A-Za-z0-9._-]+(:[0-9]{1,5})?(/[A-Za-z0-9._~/-]*)?$` using `[[ ... =~ ... ]]`; on no-match `log_error` and `exit 1`. Reject-everything-else, never a blocklist. This ordering is what makes the guard testable in a sandbox with no podman, and it is what the guard test asserts.
   - Then `ensure_podman_ready`.
   - Then `log_info` the endpoint in use, plus an explicit warning that the in-sandbox subscription OAuth token at `~/.claude/.credentials.json` remains present while Claude Code is pointed at a non-Anthropic endpoint (see the threat register, T-V3J-01) and that reaching the endpoint additionally requires the operator to have enabled the opt-in `local_model_egress` block in `policy.yaml`.
   - Then exec, mirroring the `claude` arm exactly: `openshell sandbox exec --name "${SANDBOX_NAME}" --tty --workdir "${SHARED_DIR}" --` followed by `env ANTHROPIC_BASE_URL="${LOCAL_MODEL_BASE_URL}" claude --dangerously-skip-permissions --plugin-dir /opt/claude-engineering-toolkit`. Use `env` rather than an unverified `openshell` env flag, and keep everything in argv form after `--` so no shell re-parses the value. Wrap in `set +e` / `set -e`, capture `exec_rc=$?`, emit the failure hint only when rc is neither 0 nor 130, and `exit "${exec_rc}"`.
6. *Closing summary* (the "Other verbs:" list, lines 701-705): add a `claude-local` line.

Add a comment above the arm anchoring it to `RUN-07` and noting that setting `ANTHROPIC_BASE_URL` at exec time for an explicit opt-in verb is distinct from the `ENV ANTHROPIC_BASE_URL` in the Dockerfile that `CLAUDE.md`'s What-NOT-to-Use table forbids — the default `claude` verb still sets nothing.

**B. `rebuild.sh` — `assert_local_model_egress_if_present()` (NET-06).** New function defined immediately after `assert_claude_egress_allowlist`, and **called** from the rebuild path right after the existing `assert_claude_egress_allowlist "${SANDBOX_NAME}"` at line 681. Defining without wiring the call is the failure mode this task most needs to avoid — the verify greps for both.

Behavior:
- Self-contained guarded fetch, copying the two-step pattern verbatim: `if ! policy_json=$(openshell policy get ... 2>&1); then log_error; exit 1; fi`, then `if ! echo "${policy_json}" | jq empty ...; then log_error; exit 1; fi`. A failed or non-JSON fetch aborts — it must never read as "block absent".
- Presence probe: `jq -e` selecting a network policy whose `.value.name == "local-model-egress"`. If absent, `log_info` that the opt-in local-model allow is not enabled (default posture) and `return 0`.
- If present, assert each of the following and `exit 1` with a `NET-06 VIOLATION:` message on failure:
  - `endpoints` is non-empty.
  - No endpoint carries a `protocol` field (select `.protocol != null` must find nothing).
  - No endpoint still uses the `REPLACE-ME-LOCAL-MODEL-HOST` placeholder — the operator uncommented the template without filling in the real host.
  - Every entry in `binaries[]` matches `.*/claude$` (i.e. selecting a binary whose path does **not** match must find nothing). This is the cross-scoping guard: a Go binary or any other executable scoped here would break OAuth-token isolation.
- On success, `log_info` a `NET-06 PASS:` line naming the allowed host(s).

**C. `tests/test-local-model-guard.sh`.** Follow `tests/test-pin-held.sh` house style: `#!/usr/bin/env bash`, `set -euo pipefail`, `SCRIPT_DIR`/`PROJECT_ROOT` from `BASH_SOURCE`, `PASS:`/`FAIL:` prefixed result lines to stderr, a `VIOLATIONS` counter, and a single hard gate (`exit 1` if `VIOLATIONS -gt 0`) at the end. It mutates nothing — it only invokes `rebuild.sh` with bad input and asserts exit codes.

Each of the five cases in `<behavior>` is one check. Capture rc without aborting under `set -e` (`"${PROJECT_ROOT}/rebuild.sh" claude-local >/dev/null 2>&1 || rc=$?` with `rc=0` reset before each), assert the exact expected code, and increment `VIOLATIONS` via the `VIOLATIONS=$(( VIOLATIONS + 1 ))` form — never `(( x++ ))`. For the no-`--base-url` case, run it with `LOCAL_MODEL_BASE_URL` explicitly cleared in the child environment so a set env var on the operator's machine cannot make the test pass vacuously. Add a header comment anchoring the test to `RUN-07`/`NET-06` and stating that it proves the guard, not the happy path (the happy path needs a host and cannot be tested here).

`chmod +x tests/test-local-model-guard.sh`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -n rebuild.sh && bash -n tests/test-local-model-guard.sh && echo "PASS: syntax" && S=$(grep -v '^[[:space:]]*#' rebuild.sh) && [ "$(echo "$S" | grep -c 'assert_local_model_egress_if_present')" -ge 2 ] && echo "PASS: NET-06 assertion defined AND called" && [ "$(echo "$S" | grep -c 'claude-local')" -ge 4 ] && echo "PASS: verb wired in allowlist+usage+dispatch" && echo "$S" | grep -q 'ANTHROPIC_BASE_URL' && echo "$S" | grep -q 'REPLACE-ME-LOCAL-MODEL-HOST' && echo "$S" | grep -q 'local-model-egress' && echo "PASS: sentinel + policy name linked" && bash tests/test-local-model-guard.sh && echo "PASS: guard test green" && ./rebuild.sh bogusverb >/dev/null 2>&1; [ $? -eq 1 ] && echo "PASS: unknown verb still exits 1"</automated>
  </verify>
  <done>
`bash -n` passes on both files. `assert_local_model_egress_if_present` appears at least twice in non-comment lines (definition + call site after Step 5's `assert_claude_egress_allowlist`). `claude-local` appears in the verb allowlist case, both usage strings, the dispatch arm, and the closing summary. `tests/test-local-model-guard.sh` exits 0, proving all five negative cases exit 1 from the validator without podman/openshell present. `./rebuild.sh bogusverb` still exits 1.
  </done>
  <reversibility rating="reversible">Pure repo change, no host or sandbox state mutated; `git revert` fully undoes it and the default posture is unchanged either way.</reversibility>
</task>

<task type="auto">
  <name>Task 3: docs/local-models-guidelines.md + doc-index reconciliation (D-02/D-03/D-04)</name>
  <files>docs/local-models-guidelines.md, docs/security-guidelines.md, AGENTS.md, README.md</files>
  <read_first>
    An existing guideline (e.g. `docs/integration-guidelines.md`) for house voice, heading depth, and table style. `AGENTS.md` "Docs index" table and "Project orientation" paragraph. `README.md` "Documentation" table, "Available verbs" block, and "Widening the allowlist (if needed)" section.
  </read_first>
  <action>
**A. New `docs/local-models-guidelines.md`** — the sixth domain guideline, matching the existing five in voice and density. It must carry exactly these six `##` headings, in this order:

1. `## What this repo ships (sandbox side)` — the commented `local_model_egress` template in `policy.yaml`, the `claude-local` verb and its `--base-url` / `LOCAL_MODEL_BASE_URL` inputs with the validation regex, the `NET-06` conditional assertion and each condition it fails closed on, and `tests/test-local-model-guard.sh`. State plainly that this is everything the repo provides — the proxy itself is the operator's to build (D-03).

2. `## Host-side proxy: what you must build` — the actionable setup guide, written as advice for work done outside the sandbox. Cover: the proxy must expose an **Anthropic Messages API** surface, principally `POST /v1/messages` with SSE streaming, because that is what Claude Code speaks once `ANTHROPIC_BASE_URL` is repointed; it must translate that to the switcher's **OpenAI-compatible `POST /v1/chat/completions`** (D-02, the common LiteLLM-style case). Enumerate the translation seams that actually bite: system prompt handling (Anthropic's top-level `system` vs an OpenAI `role: system` message), `messages` content blocks vs plain strings, `tool_use`/`tool_result` vs `tool_calls`/`role: tool`, `stop_reason` vs `finish_reason`, `usage` token field names, and the SSE event-name differences between the two streaming formats. Also note `GET /v1/models` is commonly probed. Bind the listener to loopback on the host. Finish with an explicit subsection on the alternative: if the switcher turns out to expose llama.cpp's **native** endpoint instead of an OpenAI-compatible one, the proxy's downstream half changes to llama.cpp's own completion API (prompt-template assembly and its own streaming shape) while the upstream Anthropic half is unchanged — state that this is the fork in the road the operator resolves by checking their switcher first (D-02).

3. `## Host reachability from the sandbox` — the known gap (D-03). State that the address the sandbox uses to reach a host-side listener under Podman + OpenShell is **unconfirmed** and that this plan deliberately guesses nothing. List the candidates worth testing (a `host.containers.internal`-style alias, the podman bridge gateway address, an explicitly published host interface) as things to verify, not as recommendations. Describe how to confirm empirically once the block is enabled, and note that whatever is confirmed must be written into **both** the `policy.yaml` host field and the `--base-url` value, and that the two must agree.

4. `## Enabling the opt-in egress allow` — the operator runbook: confirm the switcher protocol, build and run the proxy on the host, confirm the reachable address, edit `policy.yaml` (uncomment the block by stripping the leading `# ` from each template line, replace the placeholder host and the example port), re-run `./rebuild.sh` and expect the `NET-06 PASS` line, then `./rebuild.sh claude-local --base-url <url>`. Note that NET-05's deny-posture assertions are unaffected, and that `./rebuild.sh down` plus re-comment restores the default posture.

5. `## Security posture and trade-offs` — why this is a third, independently scoped allowlist rather than a widening of `claude_egress` (D-01, following the `go_egress` precedent), and why it ships commented-out. Then the honest trade-off: allowing the `claude` binary to reach an operator-controlled endpoint means the subscription OAuth token in the sandbox is now reachable by a process that also talks to a non-Anthropic host, so the isolation argument is weaker than the default two-allowlist model. Recommend running local-model sessions on a sandbox that has not completed OAuth login where practical, and keeping the proxy on loopback. Note that the endpoint is likely plaintext HTTP on the host, so prompt content is unencrypted on that hop — accepted because it does not leave the machine. Finally, reconcile the apparent conflict: `CLAUDE.md`'s What-NOT-to-Use table forbids a Dockerfile `ENV ANTHROPIC_BASE_URL`, and `.planning/PROJECT.md` describes Architecture B as having no base-URL override; this feature sets the variable **only** at exec time under an explicit opt-in verb, leaves the default `claude` verb untouched, and does not introduce an `ANTHROPIC_API_KEY`, an `inference.local` gateway, or a host-side provider. Say this explicitly so a future agent does not read the change as a violation.

6. `## Unconfirmed — verify before relying on this` — a table of open items with what to check and the consequence of the assumption being wrong. At minimum: the switcher's actual protocol (D-02); the host-reachable address and port (D-03); whether Claude Code additionally requires an auth env var (an `ANTHROPIC_AUTH_TOKEN`-style value) alongside `ANTHROPIC_BASE_URL` when the endpoint is not Anthropic's, and how that interacts with the existing subscription OAuth credential — untested from inside this sandbox; and which model identifier strings the proxy must accept and map onto the switcher's model names. Mark each as operator-verified, not repo-verified.

**B. `docs/security-guidelines.md`** — add a numbered rule to the "Editing `policy.yaml`" list covering the opt-in local-model allow: it is the one sanctioned way to add a host, it ships commented-out, it is `claude`-binary-scoped with no `protocol` field, its host is never guessed, and NET-06 asserts it fail-closed; link to `docs/local-models-guidelines.md`. Add a matching item to the "Pre-merge checklist" so the rule is enforced at review time. Do not soften rule 2 ("Never widen an allowlist") — frame this as the single documented, opt-in, default-off exception.

**C. `AGENTS.md`** — add a row to the "Docs index" table for `docs/local-models-guidelines.md` ("Read it before you… point Claude Code at a host-side local-model proxy, touch the `local_model_egress` block, or change the `claude-local` verb"). Add one sentence to the "Project orientation" networking paragraph noting the optional, default-off, `claude`-scoped third allowlist. Do not restate the guideline's contents — the file is an index (per its own instruction).

**D. `README.md`** — add a row to the "Documentation" table; add `./rebuild.sh claude-local --base-url <url>` to the "Available verbs" code block with a bullet in the list beneath it (mirroring the existing `claude` bullet); and add a short "Running against a local model (opt-in)" section after "Running Claude autonomously" that gives the three-line summary and defers to the guideline. Keep it operator-facing — no duplication of the translation detail.

Cross-check before finishing: every `docs/*.md` path referenced by any file you touched resolves to a file that exists.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && D=docs/local-models-guidelines.md && test -f "$D" && for h in "## What this repo ships (sandbox side)" "## Host-side proxy: what you must build" "## Host reachability from the sandbox" "## Enabling the opt-in egress allow" "## Security posture and trade-offs" "## Unconfirmed — verify before relying on this"; do grep -qF "$h" "$D" || { echo "FAIL: missing heading: $h"; exit 1; }; done && echo "PASS: all 6 headings" && grep -qF '/v1/messages' "$D" && grep -qF '/v1/chat/completions' "$D" && grep -qF 'ANTHROPIC_BASE_URL' "$D" && grep -qF 'llama.cpp' "$D" && echo "PASS: protocol guidance present" && grep -qF 'local-models-guidelines.md' AGENTS.md && grep -qF 'local-models-guidelines.md' README.md && grep -qF 'local-models-guidelines.md' docs/security-guidelines.md && grep -qF 'claude-local' README.md && echo "PASS: index + verb references" && for f in $(grep -rhoE 'docs/[a-z0-9-]+\.md' AGENTS.md README.md docs/*.md | sort -u); do test -f "$f" || { echo "FAIL: dangling doc link: $f"; exit 1; }; done && echo "PASS: no dangling doc links"</automated>
  </verify>
  <done>
`docs/local-models-guidelines.md` exists with all six required headings and covers `POST /v1/messages`, `/v1/chat/completions`, the llama.cpp-native alternative, `ANTHROPIC_BASE_URL`, and an explicit unconfirmed-items table. `AGENTS.md`, `README.md`, and `docs/security-guidelines.md` all reference the new guideline; `README.md` documents the `claude-local` verb. No `docs/*.md` link in any touched file is dangling.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → `rebuild.sh` | `--base-url` / `LOCAL_MODEL_BASE_URL` is untrusted input that flows into an `openshell sandbox exec` argv |
| sandbox `claude` binary → host-side proxy | new, opt-in egress hole out of the sandbox to an operator-controlled endpoint on the host |
| `policy.yaml` (operator-edited) → live OpenShell policy | hand-edited YAML becomes the enforced network policy at `sandbox create` time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-V3J-01 | Information Disclosure | `claude-local` verb + `local_model_egress` | high | mitigate | Allow ships commented-out (default posture unchanged); scoped to the `claude` binary only; Task 2 logs an explicit OAuth-token warning at launch; Task 3 documents the weakened isolation argument and recommends a non-OAuth'd sandbox for local-model sessions |
| T-V3J-02 | Tampering | `--base-url` → `openshell sandbox exec` argv | high | mitigate | Allowlist regex `^https?://[A-Za-z0-9._-]+(:[0-9]{1,5})?(/[A-Za-z0-9._~/-]*)?$` rejects whitespace, quotes, backslashes and shell metacharacters before first use; argv form after `--` so no shell re-parses the value; no `eval`; `tests/test-local-model-guard.sh` locks the guard with two injection-shaped negative cases |
| T-V3J-03 | Elevation of Privilege | operator uncomments template without filling the host in | medium | mitigate | NET-06 aborts the rebuild if `REPLACE-ME-LOCAL-MODEL-HOST` survives in the live policy, or if `endpoints` is empty |
| T-V3J-04 | Information Disclosure | `protocol: rest` added to the new endpoint | high | mitigate | Template omits `protocol` and says why; NET-06 fails closed if any `local-model-egress` endpoint carries a `protocol` field, matching the existing NET-04 checks |
| T-V3J-05 | Elevation of Privilege | cross-scoping a non-`claude` binary into `local_model_egress` | medium | mitigate | NET-06 asserts every `binaries[].path` matches `.*/claude$`; existing NET-04 cross-scoping assertions for `claude_egress`/`go_egress` are left intact |
| T-V3J-06 | Spoofing | plaintext HTTP hop from sandbox to host proxy | medium | accept | Traffic never leaves the operator's machine; guideline directs the proxy to bind loopback and documents the unencrypted hop as an accepted trade-off |
| T-V3J-07 | Tampering | supply chain | n/a | accept | No `npm`/`pip`/`cargo` install is added by this change — no new packages enter the image; the Package Legitimacy Gate is not applicable |

Every mitigation above is asserted fail-closed in the same commit as the claim it defends, per `docs/security-guidelines.md` "NET-04 / NET-05 assertion discipline".
</threat_model>

<verification>
Run from the repo root, inside the sandbox (no `podman`/`openshell`/`shellcheck`/`yq`/PyYAML available — all checks are static):

1. `bash -n rebuild.sh && bash -n tests/test-local-model-guard.sh` — syntax clean.
2. `bash tests/test-local-model-guard.sh` — exits 0; all five negative cases exit 1.
3. Default-posture regression: the comment-stripped `network_policies` region of `policy.yaml` still yields exactly `claude_egress go_egress`.
4. Link integrity: no `docs/*.md` reference in `AGENTS.md`, `README.md`, or `docs/*.md` is dangling.
5. `./rebuild.sh bogusverb` exits 1; `./rebuild.sh status` still reaches its `preflight_tools` failure (not an arg-parsing failure) — proving the flag-loop changes did not break existing verbs.

<human-check>
Operator review before this is relied upon (cannot be automated from inside the sandbox):
- The `## Host-side proxy: what you must build` section is actually actionable against your existing switcher.
- The `## Unconfirmed` table matches what you know: switcher protocol, host-reachable address, whether an auth env var is needed alongside `ANTHROPIC_BASE_URL`.
- You accept the T-V3J-01 trade-off (OAuth token present in a sandbox whose `claude` binary can now reach a host endpoint).
</human-check>
</verification>

<success_criteria>
- Default posture is provably unchanged: active `network_policies` are exactly `claude_egress` and `go_egress`; NET-04 and NET-05 are untouched.
- `policy.yaml` carries a commented, correctly indented, `claude`-scoped, protocol-free `local_model_egress` template using a placeholder host — no IP is guessed.
- `./rebuild.sh claude-local` exists, validates `--base-url` fail-closed before any host tool is invoked, and execs `claude` with `ANTHROPIC_BASE_URL` set via `env` in argv form.
- `assert_local_model_egress_if_present` is defined **and** called from the rebuild path; it fails closed on a surviving placeholder host, an empty endpoint list, any `protocol` field, or any non-`claude` binary scope.
- `tests/test-local-model-guard.sh` passes and covers five negative cases.
- `docs/local-models-guidelines.md` exists with all six required sections, gives an actionable host-side build guide, and marks the switcher protocol, the host address, and the auth-env-var question as operator-verified.
- `AGENTS.md`, `README.md`, and `docs/security-guidelines.md` reference the new guideline and verb; no dangling doc links.
- No host-side proxy code was written, run, or tested (D-03).
- Commit follows the repo convention: `feat(quick-260730-v3j): …` with the `Co-Authored-By` trailer, and the body references `NET-06` / `RUN-07`.
</success_criteria>

<output>
Create `.planning/quick/260730-v3j-add-support-for-locally-hosted-llama-cpp/260730-v3j-SUMMARY.md` when done.
</output>
