---
phase: quick-260618-qr4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [rebuild.sh, README.md]
autonomous: true
requirements: [NET-03, D-04, D-07]
must_haves:
  truths:
    - "Full rebuild configures the inference provider + gateway model idempotently at Step 0 before sandbox create — no manual one-time operator setup required (only host Claude login)."
    - "./rebuild.sh --set-model <id> switches the gateway model without a full rebuild and exits 0 after verify."
    - "Both --model and --set-model ids are allowlist-validated (^claude-[A-Za-z0-9._-]+$) before reaching any openshell command; invalid ids error + exit 1."
    - "The existing check_inference_provider gateway-section-only awk parse is preserved byte-for-byte."
  artifacts:
    - path: "rebuild.sh"
      provides: "ensure_inference_provider function, --model/--set-model flags, model-id validation, mode wiring"
      contains: "ensure_inference_provider"
    - path: "README.md"
      provides: "Updated operator docs: setup automated by rebuild.sh, --model/--set-model documented"
  key_links:
    - from: "rebuild.sh Step 0"
      to: "openshell inference set --provider claude-code --model"
      via: "ensure_inference_provider"
      pattern: "inference set --provider claude-code --model"
    - from: "rebuild.sh --set-model"
      to: "exit 0 after ensure+check"
      via: "SET_MODEL_MODE branch"
      pattern: "SET_MODEL"
---

<objective>
Automate inference provider + gateway model configuration inside rebuild.sh so the operator
no longer runs a manual one-time setup (they only need to be logged into Claude Code on the
host). Add a --model flag (default claude-opus-4-8) used by full rebuild, and a --set-model
fast-switch mode that configures inference only and exits without a full rebuild.

Purpose: The gateway hard-overrides the model (single model per gateway, no on-the-fly
multi-model — by design). Automating provider create/update at Step 0 makes rebuild
self-sufficient; --set-model is the lightweight model-switch mechanism.

Output: Modified rebuild.sh (new flags, ensure_inference_provider, mode wiring) and updated
README.md documenting the automation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@rebuild.sh
@README.md
</context>

<authoritative_spec_note>
All openshell/podman command shapes are CONFIRMED against OpenShell source — do NOT re-derive:
- Provider existence: `openshell provider get claude-code` (non-zero if absent) OR `openshell provider list --names | grep -qx claude-code`
- Absent → `openshell provider create --name claude-code --type claude-code --from-existing` (tolerate AlreadyExists)
- Present → `openshell provider update claude-code --from-existing` (re-sync — NOTE: `openshell provider refresh` only has status/configure subcommands, NO refresh-now verb)
- Route: `openshell inference set --provider claude-code --model "${MODEL}"` (user-facing route; NOT --system)
- Podman autostart: prefer `podman machine inspect --format '{{.State}}'` == running check, else `podman machine start`
- `--from-existing` reads host Claude OAuth (~/.claude/.credentials.json); failure → actionable error (operator must log into Claude Code on host) + exit 1
- Existing BUILD_DATE allowlist pattern to mirror — scripts/build-and-lock.sh:69-73 (`if ! [[ "${X}" =~ ^...$ ]]; then ... exit 1`)
</authoritative_spec_note>

<tasks>

<task type="auto">
  <name>Task 1: Add --model/--set-model flags, model-id validation, ensure_inference_provider, and mode wiring to rebuild.sh</name>
  <files>rebuild.sh</files>
  <action>
Modify rebuild.sh as follows. PRESERVE the existing check_inference_provider function (lines 59-86) and its gateway-section-only awk parse EXACTLY — do not touch it.

(a) Defaults block (near MODEL/COOLDOWN_DAYS, ~line 196): add `MODEL="claude-opus-4-8"` and `SET_MODEL_MODE=false`.

(b) Arg-parsing loop (the existing `while [[ $# -gt 0 ]]` case, ~lines 204-229): add two cases mirroring the existing --cooldown-days two-form (space and =) style:
  - `--model` / `--model=*`: set MODEL.
  - `--set-model` / `--set-model=*`: set MODEL and SET_MODEL_MODE=true.
Update the usage strings in the `*)` unknown-arg branch to include `[--model <id>]` and `--set-model <id>`.

(c) Model-id validation: after the arg loop completes, validate MODEL against the allowlist regex `^claude-[A-Za-z0-9._-]+$` — mirror the T-02-01 BUILD_DATE pattern at scripts/build-and-lock.sh:69-73 (rationale: MODEL flows into an openshell command; reject anything outside the allowlist before it reaches any CLI). On mismatch: log_error with the rejected value + exit 1. This single validation covers both --model and --set-model since both write MODEL.

(d) New function `ensure_inference_provider` — define it ABOVE the existing check_inference_provider, in the function section. Implement the three numbered steps from the authoritative spec: (1) podman autostart — check `podman machine inspect --format '{{.State}}'` for "running"; if not running OR `openshell inference get` is unreachable, run `podman machine start`, then re-check; if still unreachable, clear error + exit 1; (2) provider create-or-update — detect existence via `openshell provider get claude-code` (or list+grep -qx), absent → `openshell provider create --name claude-code --type claude-code --from-existing` (tolerate AlreadyExists in output), present → `openshell provider update claude-code --from-existing`; if --from-existing fails for lack of host login, clear actionable error ("log into Claude Code on the host") + exit 1; (3) `openshell inference set --provider claude-code --model "${MODEL}"`. Add a header comment noting the gateway hard-overrides the model (single model per gateway, by design) and that --set-model is the switch mechanism. Keep the function idempotent and fail-closed (exit 1 on unrecoverable conditions).

(e) Mode wiring:
  - Full rebuild: insert `ensure_inference_provider` at Step 0 BEFORE the existing `check_inference_provider` call (~line 253). Order: ensure_inference_provider → check_inference_provider → continue existing Step 1+.
  - --set-model: after the preflight tool-check and BEFORE BUILD_DATE compute, add a branch `if [[ "${SET_MODEL_MODE}" == "true" ]]; then ensure_inference_provider; check_inference_provider; log_info "Gateway model set to ${MODEL}; start a new Claude session to use it"; exit 0; fi`. This skips image build, teardown, sandbox create, and the NET-04/NET-05/round-trip gates.

Do NOT modify check_inference_provider's awk block. Do NOT add any direct Anthropic egress — only inference.local gateway config (zero-egress preserved).
  </action>
  <verify>
    <automated>bash -n /Users/patrickheckenlively/git/personal-sandbox/rebuild.sh && grep -q 'ensure_inference_provider' /Users/patrickheckenlively/git/personal-sandbox/rebuild.sh && grep -q 'claude-opus-4-8' /Users/patrickheckenlively/git/personal-sandbox/rebuild.sh && grep -q 'SET_MODEL_MODE' /Users/patrickheckenlively/git/personal-sandbox/rebuild.sh && grep -q 'inference set --provider claude-code --model' /Users/patrickheckenlively/git/personal-sandbox/rebuild.sh && grep -q '\^claude-\[A-Za-z0-9' /Users/patrickheckenlively/git/personal-sandbox/rebuild.sh</automated>
  </verify>
  <done>
bash -n passes; ensure_inference_provider defined and called at Step 0 before check_inference_provider; --model defaults to claude-opus-4-8; --set-model runs ensure+check then exits 0; both ids allowlist-validated (^claude-[A-Za-z0-9._-]+$); existing check_inference_provider awk parse unchanged. NOTE: live openshell/podman flow requires host stud — operator-run, not Claude-run.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md to document automated provider setup, --model, and --set-model</name>
  <files>README.md</files>
  <action>
Update README.md to reflect that inference provider setup is now automated by rebuild.sh:

(a) Options block (~lines 20-23): add `[--model <id>]` to the rebuild usage line and a new line documenting `./rebuild.sh --set-model <id>` as an inference-only fast-switch (configures provider + gateway model, then exits without a full rebuild). Note the default model is claude-opus-4-8.

(b) "What the rebuild does" Step 0 (~lines 27-30): rewrite to say Step 0 now ENSURES the provider + gateway model are configured (create-or-update, idempotent) — not just asserts. State the only host prerequisite is being logged into Claude Code on the host (so `--from-existing` can read OAuth state); rebuild.sh does the rest. Mention podman autostart.

(c) "One-time inference provider setup" section (~lines 91-121): retitle/reframe to make clear this is now AUTOMATED by rebuild.sh — the operator only needs to be logged into Claude Code on the host (no manual `openshell provider create` / `inference set` required). Keep the conceptual NET-03/D-04 explanation (gateway injects the real subscription credential host-side; no ANTHROPIC_API_KEY). You may keep the raw commands as "what rebuild.sh runs for you under the hood" reference, but the operator action reduces to host Claude login.

(d) Document the gateway single-model behavior: the gateway hard-overrides the model (one model per gateway by design); use `./rebuild.sh --set-model <id>` to switch, then start a NEW Claude session to pick up the change.

(e) PRESERVE the existing note that "System inference: Not configured is expected" / the gateway-route-only rationale wherever it appears — do not remove it.

Keep zero-egress framing intact — only inference.local gateway config, never direct Anthropic egress.
  </action>
  <verify>
    <automated>grep -q -- '--set-model' /Users/patrickheckenlively/git/personal-sandbox/README.md && grep -q -- '--model' /Users/patrickheckenlively/git/personal-sandbox/README.md && grep -q 'claude-opus-4-8' /Users/patrickheckenlively/git/personal-sandbox/README.md</automated>
  </verify>
  <done>
README documents --model (default claude-opus-4-8) and --set-model fast-switch; "One-time setup" reframed as automated (operator only needs host Claude login); single-model gateway behavior + "new session to switch" documented; NET-03/D-04 credential-injection explanation and "System inference: Not configured is expected" note preserved.
  </done>
</task>

</tasks>

<verification>
- `bash -n rebuild.sh` passes (syntax clean).
- ensure_inference_provider defined above and called before the unmodified check_inference_provider.
- Model-id allowlist validation present for both flags.
- --set-model branch exits 0 without build/teardown/create/NET gates.
- README documents both flags and the automated setup; preserved notes intact.
- The live openshell/podman provider/inference flow cannot be exercised by Claude (needs host
  Claude login + podman machine + gateway) — flag as operator-run validation: operator runs
  `./rebuild.sh` (full) and `./rebuild.sh --set-model <id>` on the host to confirm.
</verification>

<success_criteria>
- rebuild.sh: --model (default claude-opus-4-8) and --set-model <id> implemented with allowlist validation.
- ensure_inference_provider idempotently creates-or-updates the provider, autostarts podman, and sets the gateway model; fail-closed with actionable errors.
- Full rebuild wires ensure_inference_provider → check_inference_provider → Step 1+.
- --set-model runs ensure+check+verify then exits 0 with the "start a new Claude session" message.
- Existing check_inference_provider gateway-section-only awk parse byte-for-byte unchanged.
- Zero-egress preserved; README updated.
</success_criteria>

<output>
Create `.planning/quick/260618-qr4-automate-inference-provider-setup-in-reb/260618-qr4-SUMMARY.md` when done.
</output>
