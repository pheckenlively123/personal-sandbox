---
phase: 04-claude-code-launch-and-mcp-audit
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - rebuild.sh
  - scripts/audit-plugins.sh
  - .planning/phases/04-claude-code-launch-and-mcp-audit/PLUGIN-AUDIT.md
autonomous: false
requirements: [RUN-01, RUN-02]
must_haves:
  truths:
    - "An operator can run the full plugin audit with a single verb: ./rebuild.sh audit-plugins"
    - "Each of the 11 agents + 6 skills is invoked once headless and reaches a terminal state (no exit-124 hang)"
    - "MUST_SUCCEED plugins exit 0 with output; MUST_FAIL_CLEAN plugins exit 0 with a clean network/MCP error"
    - "Any expected/actual mismatch — including a MUST_FAIL_CLEAN plugin that exits 0 without a clean network/MCP error — increments VIOLATIONS and hard-fails the audit (exit non-zero) (D-10)"
    - "statsig.anthropic.com and sentry.io produce zero claude.exe denial entries during the run (telemetry suppressed, criterion #3)"
    - "A committed PLUGIN-AUDIT.md records per-plugin verdicts + wall-clock + telemetry evidence"
  artifacts:
    - path: "scripts/audit-plugins.sh"
      provides: "headless audit harness (D-04) — static plugin enumeration, per-plugin claude -p, telemetry check, hard-fail"
      contains: "MUST_FAIL_CLEAN"
    - path: "rebuild.sh"
      provides: "new `audit-plugins` verb (D-05) delegating to scripts/audit-plugins.sh"
      contains: "audit-plugins"
    - path: ".planning/phases/04-claude-code-launch-and-mcp-audit/PLUGIN-AUDIT.md"
      provides: "committed audit report (D-06) — criterion #2 + #3 evidence"
      contains: "vuln-reviewer"
  key_links:
    - from: "rebuild.sh audit-plugins verb"
      to: "scripts/audit-plugins.sh"
      via: "bash delegation (thin wrapper)"
      pattern: "audit-plugins.sh"
    - from: "scripts/audit-plugins.sh"
      to: "openshell sandbox exec --no-tty --timeout 120 -- claude -p"
      via: "per-plugin headless invocation"
      pattern: "--timeout 120"
    - from: "scripts/audit-plugins.sh telemetry check"
      to: "openshell logs --source sandbox"
      via: "grep DENIED.*claude.exe.*statsig|sentry"
      pattern: "DENIED.*claude\\.exe"
---

<objective>
Deliver the reproducible plugin audit (criterion #2) and the telemetry-suppression evidence (criterion #3) as a new `./rebuild.sh audit-plugins` verb backed by `scripts/audit-plugins.sh`, producing a committed PLUGIN-AUDIT.md report that hard-fails on any expected/actual mismatch.

Purpose: D-04/D-05 — a scripted headless harness (not a manual checklist), exposed as a verb, mirroring the verify-pins.sh fail-closed discipline. D-08 — static enumeration of the 11 agents + 6 skills (more fail-closed than parsing plugin.json at runtime). D-09/D-10 — a per-plugin expected-outcome table (local-tool agents + full-review/review-fix-loop/agent-readiness MUST_SUCCEED; vuln-reviewer + jira-ticket/implement/my-work MUST_FAIL_CLEAN), hard-failing on ANY mismatch (including a MUST_FAIL_CLEAN plugin that exits 0 without a clean network/MCP error — that too is a mismatch). D-07 — distinguish a blocked-host hang from legitimate model latency via `openshell sandbox exec --timeout 120` (proxy returns 403 in ~38ms; exit 124 = hang = always FAIL; model latency legitimately exceeds 10s). D-11/D-12 — fold the telemetry check into the same run: assert zero claude.exe denials to statsig/sentry; document mcp-proxy/datadog denials as expected policy enforcement. D-06 — commit the report into the phase dir.
Output: `audit-plugins` verb, scripts/audit-plugins.sh, and the committed PLUGIN-AUDIT.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/04-claude-code-launch-and-mcp-audit/04-RESEARCH.md
@.planning/phases/04-claude-code-launch-and-mcp-audit/04-CONTEXT.md
@rebuild.sh
@scripts/verify-pins.sh
</context>

<artifacts_this_phase_produces>
This plan (04-03) produces:
- `scripts/audit-plugins.sh`: self-contained headless audit harness (logging helpers copy-pasted, not imported — existing convention). Static `AGENTS` + `SKILLS` associative arrays with expected verdicts; per-plugin `openshell sandbox exec --no-tty --timeout 120 -- claude -p` loop; wall-clock capture; output pattern-match for MUST_FAIL_CLEAN; telemetry check via `openshell logs`; `VIOLATIONS` counter; exit non-zero on any violation (D-10).
- `rebuild.sh`: new `audit-plugins` verb (added to the `case "$1"` whitelist, the `case "${VERB}"` dispatch, and usage/help) delegating to `scripts/audit-plugins.sh` (D-05).
- `.planning/phases/04-claude-code-launch-and-mcp-audit/PLUGIN-AUDIT.md`: committed report (D-06) — per-plugin table + telemetry evidence.
Depends on 04-02 (which also edits rebuild.sh) and 04-01 (rebuilt sandbox with /opt readable + govulncheck on PATH).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/audit-plugins.sh — headless harness with static enumeration, telemetry check, hard-fail (D-04/D-07/D-08/D-09/D-10/D-11)</name>
  <files>scripts/audit-plugins.sh</files>
  <read_first>
    - scripts/verify-pins.sh lines 1-60 (fail-closed header style: `set -euo pipefail`, SCRIPT_DIR/PROJECT_ROOT, ERROR: discipline, argument parsing — mirror this)
    - rebuild.sh lines 48-55 (ts / log_step / log_info / log_error helpers to copy-paste in, per the self-contained-script convention)
    - 04-RESEARCH.md "Per-Plugin Expected-Outcome Table" (the exact 11 agents + 6 skills with MUST_SUCCEED/MUST_FAIL_CLEAN verdicts and reasons), "Plugin Enumeration Pattern (D-08)" (the AGENTS/SKILLS associative arrays), "Example: Headless Plugin Invocation in audit-plugins.sh" (run_plugin_audit — note the FAIL [MISMATCH] branch hard-fails a MUST_FAIL_CLEAN plugin that exits 0 without a network/MCP error), "Example: Telemetry Check in audit-plugins.sh" (check_telemetry_suppression), "Headless Invocation Pattern" (agent vs skill prompts), and "D-07 Resolution" (exit-124 = hang = FAIL)
  </read_first>
  <action>
    Create scripts/audit-plugins.sh, self-contained (`set -euo pipefail`, copy-paste the ts/log_step/log_info/log_error helpers from rebuild.sh — do NOT source rebuild.sh, matching the verify-pins.sh convention). Accept positional args `SANDBOX_NAME` and `SHARED_DIR` (the verb passes these). Enumerate statically (D-08) via two associative arrays exactly as RESEARCH specifies: AGENTS — the 11 reviewer agents all MUST_SUCCEED except `vuln-reviewer`=MUST_FAIL_CLEAN (govulncheck needs vuln.go.dev, outside the 3-host allowlist — Option C, documented); SKILLS — `full-review`/`review-fix-loop`/`agent-readiness`=MUST_SUCCEED, `jira-ticket`/`implement`/`my-work`=MUST_FAIL_CLEAN.

    Capture a run-start marker (date/since-window) before the loop so the telemetry grep can scope to this run. For each plugin run `openshell sandbox exec --name "$SANDBOX_NAME" --no-tty --timeout 120 --workdir /claudeshared -- claude --plugin-dir /opt/claude-engineering-toolkit -p "<prompt>"` capturing combined stdout+stderr to a variable and the exit code; agents use the prompt `Run @<agent-name> on the current directory`, skills use `Run /<skill-name>`. Record wall-clock per invocation. Maintain an integer `VIOLATIONS` counter initialized to 0; every FAIL path increments it. Verdict logic (D-07/D-09/D-10): exit 124 → HANG → always FAIL (increment VIOLATIONS); exit 0 + MUST_SUCCEED → PASS; exit 0 + MUST_FAIL_CLEAN → PASS only if output matches a network/MCP error pattern (`403|connection refused|not available|tool.*not.*found|cannot connect|network unreachable`), else this is an expected/actual MISMATCH per D-10 → FAIL (increment VIOLATIONS) — do NOT record it as a non-fatal WARN; keep the human-readable log line (e.g. `FAIL [MISMATCH] <plugin>: expected clean failure but exited 0 with no network/MCP error`) AND make it count toward exit 1; any other case (e.g. exit non-zero for a MUST_SUCCEED plugin) → FAIL (increment VIOLATIONS). Apply this hard-fail-on-mismatch rule consistently to EVERY expected/actual mismatch — there is no escape that records a violation-worthy state while leaving the exit code 0. NEVER `eval` plugin output — capture to a variable and pattern-match only (V5 / threat T-04-07).

    Add a `check_telemetry_suppression` function (D-11/D-12) using `openshell logs "$SANDBOX_NAME" --source sandbox --since <run-window> -n 2000`: grep `DENIED.*claude\.exe.*statsig` and `DENIED.*claude\.exe.*sentry` — both MUST be zero (else log_error + increment VIOLATIONS, criterion #3 fail). Count and log (informationally, not as failures) `mcp-proxy.anthropic.com`, `datadoghq.com`, and `downloads.claude.ai` denials as expected policy enforcement. Guard each grep with `|| true` and filter comment lines if grepping any file. End the script with a single fail-closed gate: if `VIOLATIONS > 0` → log_error summary (count + which plugins) + `exit 1` (D-10 hard-fail); else `exit 0`. The exit-1 path MUST be gated on `VIOLATIONS > 0` — a script that defines VIOLATIONS but always exits 0 is wrong. Emit `=== [ts] Step N ===` banners per phase. The script should write or print the per-plugin results in a form Task 3 captures into PLUGIN-AUDIT.md (e.g. a results table to stdout).
  </action>
  <verify>
    <automated>bash -n scripts/audit-plugins.sh && grep -q 'MUST_FAIL_CLEAN' scripts/audit-plugins.sh && grep -q 'vuln-reviewer' scripts/audit-plugins.sh && grep -q -- '--timeout 120' scripts/audit-plugins.sh && grep -q 'DENIED.*claude' scripts/audit-plugins.sh && ! grep -q 'eval ' scripts/audit-plugins.sh && grep -qE 'VIOLATIONS' scripts/audit-plugins.sh && grep -qE 'exit[[:space:]]+1' scripts/audit-plugins.sh && grep -Eq 'VIOLATIONS[^0-9]*-(gt|ne)[^0-9]*0|VIOLATIONS[^0-9]*>[^0-9]*0|\(\(.*VIOLATIONS.*\)\)' scripts/audit-plugins.sh && echo OK</automated>
    <automated>! grep -qiE 'WARN \[CHECK\]|return 0 *# *warn|MISMATCH.*return 0' scripts/audit-plugins.sh && echo "no-warn-escape OK"</automated>
  </verify>
  <done>scripts/audit-plugins.sh exists, `bash -n` passes, statically enumerates 11 agents + 6 skills with the RESEARCH verdicts, uses `--timeout 120` + exit-124=hang logic, includes the statsig/sentry telemetry assertion, never evals plugin output, and hard-fails (exit 1) on ANY expected/actual mismatch — including a MUST_FAIL_CLEAN plugin that exits 0 without a clean network/MCP error. The final `exit 1` is gated on `VIOLATIONS > 0` (a script that always exits 0 must NOT pass); there is no WARN escape that leaves a mismatch unpenalized.</done>
</task>

<task type="auto">
  <name>Task 2: Add the `audit-plugins` verb to rebuild.sh (D-05)</name>
  <files>rebuild.sh</files>
  <read_first>
    - rebuild.sh lines 236-254 (the `case "$1"` verb whitelist — add `audit-plugins`; NOTE 04-02 already added `claude` here, read current state)
    - rebuild.sh lines 388-396 (the existing `audit)` verb — the thin-wrapper shape to mirror; `audit-plugins` is a DISTINCT verb, stricter posture per CONTEXT D-05/code_context)
    - rebuild.sh lines 246-251, 288-294 (usage strings to update)
    - 04-RESEARCH.md "Verb Pattern: `audit-plugins` Verb (D-05)"
  </read_first>
  <action>
    Add `audit-plugins` to the first-argument verb whitelist in the `case "$1"` block (it must be a literal alternative, distinct from the existing `audit` — do not let `audit` swallow it). Add an `audit-plugins)` branch to the `case "${VERB}"` dispatch, placed after the existing `audit)` branch, that delegates to the harness: `bash "${PROJECT_ROOT}/scripts/audit-plugins.sh" "${SANDBOX_NAME}" "${SHARED_DIR}"` then `exit 0` (thin wrapper per D-05). Add a log_info line noting this is the strict, hard-failing audit (distinct from the log-surfacing `audit` verb). Update all usage/help strings and the header comment block to list `audit-plugins`. Keep the existing `audit` verb unchanged. Preserve `set -euo pipefail` and helper conventions; no `eval`.
  </action>
  <verify>
    <automated>bash -n rebuild.sh && grep -q 'audit-plugins)' rebuild.sh && grep -q 'scripts/audit-plugins.sh' rebuild.sh && grep -q 'audit-plugins' rebuild.sh && echo OK</automated>
  </verify>
  <done>`./rebuild.sh audit-plugins` is a recognized, distinct verb that delegates to scripts/audit-plugins.sh; the existing `audit` verb is unchanged; `bash -n` passes; usage text lists both audit verbs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the audit, capture results into PLUGIN-AUDIT.md, confirm hard-fail gate + telemetry (criterion #2 + #3, D-06)</name>
  <what-built>The `audit-plugins` verb drives 17 headless `claude -p` invocations against the running OAuth'd sandbox (each a live api.anthropic.com round-trip) and runs the telemetry log check. This requires a human because it needs the running, logged-in sandbox from 04-01/04-02, takes several minutes of real model round-trips, and the resulting report (D-06) must be reviewed before commit.</what-built>
  <how-to-verify>
    1. Confirm prerequisites: sandbox Ready and OAuth'd (`./rebuild.sh status`; re-run `./rebuild.sh login` if needed).
    2. Run the audit, capturing full output:
       `./rebuild.sh audit-plugins 2>&1 | tee /tmp/audit-plugins.log`
       Expect: each of the 11 agents + 6 skills reaches a terminal state within 120s (NO exit-124 HANG lines). MUST_SUCCEED plugins → PASS [OK]; MUST_FAIL_CLEAN plugins (vuln-reviewer, jira-ticket, implement, my-work) → PASS [FAIL_CLEAN] with a 403/connection-refused/tool-unavailable message. The telemetry section shows TELEMETRY PASS for statsig (0 claude.exe attempts) and sentry (0), and informational denials for mcp-proxy.anthropic.com / datadoghq.com. Exit code 0 = all verdicts matched (D-10 gate green).
    3. If any plugin shows FAIL/HANG/MISMATCH, that is a finding — per D-09/D-10 it must be fixed or explicitly justified before the table is locked (do NOT just relabel it as expected). Note that a MUST_FAIL_CLEAN plugin exiting 0 WITHOUT a clean network/MCP error is a MISMATCH that hard-fails the run (exit 1) — that is intended. Capture the discrepancy in the resume signal.
    4. Create `.planning/phases/04-claude-code-launch-and-mcp-audit/PLUGIN-AUDIT.md` (D-06): a per-plugin results table (plugin, type MUST_SUCCEED/MUST_FAIL_CLEAN, observed exit, wall-clock seconds, verdict) populated from /tmp/audit-plugins.log, plus the telemetry-suppression evidence section (statsig=0, sentry=0, expected mcp-proxy/datadog denials documented per D-11), plus a one-line statement that criterion #2 (no unbounded hang; each plugin reached a terminal state) and criterion #3 (telemetry suppressed) passed. Use the RESEARCH "Per-Plugin Expected-Outcome Table" structure.
  </how-to-verify>
  <resume-signal>Type "approved" once the audit exits 0, no plugin hung (no exit 124) and no MISMATCH occurred, statsig+sentry show 0 claude.exe attempts, and PLUGIN-AUDIT.md is written. Otherwise describe the failing/hanging/mismatched plugin so it can be fixed or justified before locking the table.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host audit harness → sandbox claude -p | each invocation crosses into the sandbox; plugin output returns to the host script |
| plugin output → host shell | untrusted-ish text (model/plugin output) captured by audit-plugins.sh |
| claude.exe → 3-host allowlist | denied hosts (statsig/sentry/jira/github/google) return fast 403; logged by OCSF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | audit-plugins.sh treating plugin output as shell input | mitigate | Capture plugin output into a quoted variable and pattern-match with grep only; NEVER `eval` or re-exec plugin output (verified by the `! grep eval` gate in Task 1). |
| T-04-08 | Denial of Service | a plugin hanging on a blocked-host network call | mitigate | `openshell sandbox exec --timeout 120` bounds every invocation; exit 124 = hang = hard FAIL (D-07). Proxy returns 403 in ~38ms so blocked plugins finish in seconds. |
| T-04-09 | Information Disclosure | telemetry/auto-update egress from claude.exe | mitigate | check_telemetry_suppression asserts zero claude.exe denials to statsig/sentry (CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1 effective); policy denies all non-allowlisted hosts regardless (defense-in-depth). |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (harness uses only openshell/claude/grep already present); no legitimacy gate required. |
</threat_model>

<verification>
- `bash -n` passes for both scripts/audit-plugins.sh and rebuild.sh.
- Harness statically enumerates 11 agents + 6 skills with RESEARCH verdicts; uses `--timeout 120`; asserts statsig+sentry=0; never `eval`s plugin output; hard-fails (exit 1) on ANY violation including a MUST_FAIL_CLEAN plugin that exits 0 without a clean network/MCP error. The final `exit 1` is gated on `VIOLATIONS > 0`.
- `audit-plugins` is a distinct verb delegating to the harness; existing `audit` verb unchanged.
- Live run exits 0 with no exit-124 hangs and no MISMATCH; PLUGIN-AUDIT.md committed with per-plugin verdicts + wall-clock + telemetry evidence (criterion #2 + #3).
</verification>

<success_criteria>
- Operator capability: a single `./rebuild.sh audit-plugins` runs the full reproducible audit and hard-fails on regression (D-10), including any MUST_FAIL_CLEAN plugin that does not fail with a clean network/MCP error.
- Criterion #2 satisfied: every plugin reaches a terminal state within 120s (no unbounded hang); verdicts match the locked expected-outcome table.
- Criterion #3 satisfied: zero claude.exe attempts to statsig/sentry; telemetry suppressed, evidenced in the committed report.
- D-06 satisfied: PLUGIN-AUDIT.md committed as durable evidence and a regression guard.
</success_criteria>

<output>
Create `.planning/phases/04-claude-code-launch-and-mcp-audit/04-03-SUMMARY.md` when done.
</output>
